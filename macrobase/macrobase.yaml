
server:
    rootPath: /api/*

logging:
  level: INFO

  # Logger-specific levels.
  loggers:
    "macrobase": TRACE

macrobase.analysis.minSupport: 0.001
macrobase.analysis.minOIRatio: 2
macrobase.loader.db.user: postgres
macrobase.loader.db.password: postgres
macrobase.loader.db.database: postgres
